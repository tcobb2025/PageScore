services:
  - type: web
    name: PageScore
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 webhook.py
    plan: free
    envVars:
      - key: FLASK_PORT
        value: "5000"
      - key: PORT
        value: "5000"
